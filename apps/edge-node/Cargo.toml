[package]
name = "edge-node"
version = "0.1.0"
edition = "2021"

[dependencies]
proxy = { path = "../../core/proxy" }
telemetry = { path = "../../core/telemetry" }

tokio = { version = "1", features = ["full"] }

hyper = { version = "1", features = ["full"] }
hyper-util = { version = "0.1", features = ["full"] }

http-body-util = "0.1"
bytes = "1"